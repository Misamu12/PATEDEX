import { Text, View } from "react-native";

export default function apropos() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>A propos</Text>
    </View>
  );
}
